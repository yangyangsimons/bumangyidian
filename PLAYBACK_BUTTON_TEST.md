# 播放按钮状态同步修复测试指南

## 修复内容

### 🔧 **主要改动**

1. **将 music.js 转换为 Composition API 格式**：使用`computed`属性替代`watch`来确保状态响应式更新
2. **优化状态同步逻辑**：`isPlaying`现在是基于`audioPlayerStore`状态的计算属性
3. **添加详细日志**：帮助调试状态变化过程

### 📋 **测试步骤**

#### 测试 1: WebSocket 音乐不影响播放按钮

1. ✅ 在 index 页面播放 WebSocket 背景音乐
2. ✅ 切换到 programme 页面
3. ✅ 确认播放按钮显示为 ▶️（三角形），不是 ⏸️（暂停）

#### 测试 2: 点击播放按钮状态变化

1. ✅ 在 programme 页面点击任意音乐的播放按钮
2. ✅ 确认按钮从 ▶️ 变为 ⏸️
3. ✅ 再次点击确认从 ⏸️ 变为 ▶️

#### 测试 3: 多个播放按钮状态独立

1. ✅ 播放一首音乐，确认其按钮显示 ⏸️
2. ✅ 确认其他音乐按钮仍显示 ▶️
3. ✅ 点击另一首音乐，确认状态正确切换

#### 测试 4: 页面切换状态保持

1. ✅ 在 programme 页面播放音乐
2. ✅ 切换到其他页面再回来
3. ✅ 确认播放状态正确保持

### 🐛 **调试信息**

打开浏览器控制台，应该能看到以下日志：

- `isPlaying计算:` - 播放状态计算过程
- `isPlayingAudio检查:` - 按钮状态检查过程
- `togglePlay调用:` - 播放切换调用信息
- `开始播放歌曲:` - 播放开始信息

### 🔍 **预期行为**

- ✅ WebSocket 音乐播放时，programme 页面按钮不会错误显示为暂停状态
- ✅ 点击播放按钮后，状态立即更新（▶️ ↔ ⏸️）
- ✅ 只有当前播放的音乐按钮显示暂停状态
- ✅ 切换页面不影响状态准确性

### 🚨 **如果仍有问题**

如果播放按钮状态还是不更新，请检查：

1. 控制台是否有错误信息
2. `isPlayingAudio`方法的日志输出
3. `audioPlayerStore.bgIsPlaying`和`bgAudioId`的值变化

请测试这些场景并告诉我结果！
